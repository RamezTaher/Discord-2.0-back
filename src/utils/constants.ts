export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  CHANNELS = 'channels',
  CHANNELMEMBER = 'channel_member',
}
export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  CHANNELS = 'CHANNELS_SERVICE',
  CHANNELMEMBER = 'CHANNEL_MEMBER_SERVICE',
}
